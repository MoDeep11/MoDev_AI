[project]
name = "modev"
version = "0.1.0"
description = "AI-powered project structure generation system with FastAPI and Gemini integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
]

[project.optional-dependencies]
ai = [
    "google-genai>=1.0.0",
]
